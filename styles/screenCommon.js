/**
 * Backward-compatible re-export.
 * Canonical shared styles live in utils/screenCommon.js — import from there.
 */
export {
  GhostColors,
  GhostSpacing,
  ghostPressable,
  ghostPressedStyle,
} from '../utils/screenCommon';
